package tn.esprit.pi.services;

//import org.springframework.mail.SimpleMailMessage;
//import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Service;

@Service
public class EmailVerificationService {
/*
    private final JavaMailSender javaMailSender;

    public EmailVerificationService(JavaMailSender javaMailSender) {
        this.javaMailSender = javaMailSender;
    }

    public void sendVerificationEmail(String email) {
        SimpleMailMessage message = new SimpleMailMessage();
        message.setTo(email);
        message.setSubject("Vérification d'adresse e-mail");
        message.setText("Bonjour, veuillez cliquer sur le lien suivant pour vérifier votre adresse e-mail : [Lien de vérification]");
        javaMailSender.send(message);
    }*/
}
